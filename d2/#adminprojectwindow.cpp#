#include "adminprojectwindow.h"
#include "ui_adminprojectwindow.h"

AdminProjectWindow::AdminProjectWindow(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::AdminProjectWindow)
{
    //project=proj;
// // //    ui->gpaSpin->setValue(project->getGpa());
// // //    ui->pmSpin->setValue(project->getPmSkill());
// // //    ui->dbSpin->setValue(project->getDatabase());
// // //    ui->javaSpin->setValue(project->getJava());
// // //    ui->cSpin->setValue(project->getCCpp());
// // //    ui->jsSpin->setValue(project->getJsHtml());
// // //    ui->pythonSpin->setValue(project->getPython());
// // //    ui->debuggingSpin->setValue(project->getDebug());
// // //    ui->docSpin->setValue(project->getDocument());
// // //    ui->mobileSpin->setValue(project->getMobile());
// // //    ui->networkingSpin->setValue(project->getNetwork());
// // //    ui->uiSpin->setValue(project->getUi());
// // //    ui->algSpin->setValue(project->getAlgor());
    ui->setupUi(this);
}

AdminProjectWindow::~AdminProjectWindow()
{
    delete ui;
}
